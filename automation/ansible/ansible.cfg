[defaults]
# Relative, not absolute. These resolve against the directory ansible is run
# from, which is this one — so a fresh clone works without a toolbox VM, and
# the toolbox still works because it runs from here too. Absolute /opt/toolbox
# paths meant a clone anywhere else could not find its own roles.
roles_path = ./roles
inventory  = ./inventory/hosts.yml
