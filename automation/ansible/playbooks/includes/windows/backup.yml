---
# Windows Server Backup — install the FEATURE only. Backup targets/jobs are a
# per-server decision made later, not part of the baseline.

- name: "Install the Windows Server Backup feature"
  ansible.windows.win_feature:
    name: Windows-Server-Backup
    state: present
