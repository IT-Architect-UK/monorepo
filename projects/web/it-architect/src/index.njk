---
layout: layouts/base.njk
title: IT Infrastructure, Security & AI Automation
description: IT infrastructure and security experts — 25 years designing, building and securing enterprise infrastructure on-premises, in cloud and hybrid. Cloud migration, infrastructure as code, end user compute, zero-trust security and applied AI. UK-based.
hidePageTitle: true
---

<section class="hero">
  <div class="container">
    <p class="eyebrow">{{ site.yearsExperience }} years · enterprise infrastructure</p>
    <h1>IT Infrastructure &amp; Security experts</h1>
    <p class="lead">
      Twenty-five years designing, building and securing enterprise
      infrastructure — on-premises, cloud and hybrid, with applied AI where it
      genuinely helps.
    </p>
    <div class="hero-actions">
      <a class="btn btn-primary" href="/services/">What we do</a>
      <a class="btn btn-ghost" href="/evidence/">See the evidence</a>
    </div>
  </div>
</section>

{# No metric band here, deliberately. A row of large decontextualised figures
   ("65,000", "2,000") is unfalsifiable: it names no client, states no role and
   links to nothing, so a buyer cannot tell what part of it was ours. It read as
   a consultancy slide and carried no weight. Numbers now appear only alongside
   the engagement that produced them, where they can be judged. #}

<section class="section">
  <div class="container">
    <h2>What we do</h2>
    <p class="section-intro">
      Six areas, all of them delivered rather than read about.
    </p>
    <div class="cards">
      {% for s in services %}
      <article class="card">
        <h3><a href="/{{ s.slug }}/">{{ s.title }}</a></h3>
        <p>{{ s.summary }}</p>
      </article>
      {% endfor %}
    </div>
  </div>
</section>

<section class="section">
  <div class="container">
    <h2>Infrastructure at scale</h2>
    <p class="section-intro">
      IT Architect is led by Darren Pilkington. Before cloud was the default the
      work was racks, storage, virtualisation and migration windows, and that
      grounding is why the cloud work holds up.
    </p>
    <div class="cols">
      <div>
        <h3>Data centre build and exit</h3>
        <p>
          Data centres built and commissioned for remote offices worldwide, and
          new enterprise facilities in the UK. Later, four legacy data centres
          decommissioned with zero data loss — the full lifecycle, from empty
          room to switch-off.
        </p>
      </div>
      <div>
        <h3>Migration at volume</h3>
        <p>
          Thousands of virtual machines and business applications migrated,
          including energy trading systems, plus 2,000 servers moved to hybrid
          cloud during a platform upgrade. Large migrations are a planning and
          rollback problem long before they are a technical one.
        </p>
      </div>
      <div>
        <h3>Global estates</h3>
        <p>
          Thirteen years as a global infrastructure architect for an energy group
          operating in more than 50 countries, followed by security and identity
          programmes reaching 65,000 users across hybrid cloud.
        </p>
      </div>
    </div>

    <h3 class="tech-heading">Technologies we deliver with</h3>
    <dl class="tech-grid">
      {% for t in technologies %}
      <div class="tech-group">
        <dt>{{ t.group }}</dt>
        <dd>{{ t.items }}</dd>
      </div>
      {% endfor %}
    </dl>
  </div>
</section>

<section class="section section-dark">
  <div class="container">
    <h2>How we work</h2>
    <div class="cols">
      <div>
        <h3>Reproducible, not hand-built</h3>
        <p>
          If an environment cannot be rebuilt from source control, it is a
          liability. Everything we build is defined as code, validated in CI and
          documented for whoever inherits it.
        </p>
      </div>
      <div>
        <h3>Straight answers</h3>
        <p>
          Sometimes the right advice is that a migration is not worth it, or that
          AI will not help with the problem you have. You will get that view
          early, not after the invoice.
        </p>
      </div>
      <div>
        <h3>Security as a default</h3>
        <p>
          Hardening, identity and least privilege designed in from the start.
          Controls engineers route around are worse than none at all.
        </p>
      </div>
      <div>
        <h3>Evidence over adjectives</h3>
        <p>
          Working reference implementations are published publicly so you can
          inspect the standard of work before you commit to anything.
        </p>
      </div>
    </div>
  </div>
</section>

<section class="section">
  <div class="container">
    <div class="cta-panel">
      <h2>Got a project in mind?</h2>
      <p>
        Tell us what you are trying to achieve and you will get an honest view
        on whether we are the right fit for it.
        <a href="mailto:{{ site.email }}">{{ site.email }}</a> or
        <a href="/contact/">get in touch</a>.
      </p>
    </div>
  </div>
</section>
