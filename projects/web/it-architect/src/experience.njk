---
layout: layouts/base.njk
title: Experience
heading: Experience
standfirst: Twenty-five years of it — where the work has been done, and what it achieved.
description: Twenty-five years of enterprise infrastructure delivery across energy, aviation, financial services and IT services — data centre build and decommission, large-scale migration, zero-trust security and automation.
---

<section class="section">
  <div class="container">
    {# Personal voice here by design. These are Darren's roles at other firms,
       most of them predating IT Solution Architecture Limited — describing them
       as the company's engagements would overstate the company's track record. #}
    <p class="lead prose">
      This is Darren Pilkington's own delivery history, most of it as an architect
      and SME inside large programmes. Clients are described by sector rather than
      name: engagements were under contract and confidentiality, and I would
      rather be trusted with yours than trade on theirs.
    </p>

    <div class="timeline">
      {% for e in experience %}
      <article class="role">
        <div class="role-meta">
          <p class="role-period">{{ e.period }}</p>
          <p class="role-engagement">{{ e.engagement }}</p>
        </div>
        <div class="role-body">
          <h2>{{ e.role }}</h2>
          <p class="role-sector">{{ e.sector }}</p>
          <p>{{ e.summary }}</p>
          <ul class="ticks">
            {% for p in e.points %}<li>{{ p }}</li>{% endfor %}
          </ul>
        </div>
      </article>
      {% endfor %}
    </div>

    <div class="cta-panel">
      <h2>Full history</h2>
      <p>
        A complete employment history, endorsements and the full skills list are
        on <a href="{{ site.linkedin }}" target="_blank" rel="noopener">LinkedIn<span class="visually-hidden"> (opens in a new tab)</span></a>.
      </p>
    </div>
  </div>
</section>
