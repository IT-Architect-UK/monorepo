{
  "name": "itsurgery-website",
  "version": "1.0.0",
  "private": true,
  "description": "IT Surgery — local IT support website (Penarth, Barry, Cardiff). Static site built with Eleventy.",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
