---
# Retired. The HTML site map duplicated the footer, which already lists every
# service page. Search engines are served by /sitemap.xml instead, which is what
# they actually read. permalink:false stops this building — the file can be
# deleted outright whenever convenient.
permalink: false
eleventyExcludeFromCollections: true
---
