<!DOCTYPE html>
<html lang="en-GB">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{ title }} | {{ site.name }}</title>
  <meta name="robots" content="noindex" />
  <link rel="stylesheet" href="{{ '/assets/fonts.css' | v }}" />
  {# Each print piece brings its own sheet; the A4 leaflet is the default. #}
  <link rel="stylesheet" href="{{ stylesheet or '/assets/flyer.css' }}" />
</head>
<body>
{{ content | safe }}
</body>
</html>
